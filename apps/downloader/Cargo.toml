[package]
name = "downloader"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.48.0", features = ["full"] }
pretty_env_logger = "0.5.0"
serde = { version = "1.0.228", features = ["derive"] }

tbh-torbox = { path = "../../libs/torbox" }
tbh-config = { path = "../../libs/config" }
